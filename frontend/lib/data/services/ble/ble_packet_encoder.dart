import 'package:lifora/domain/entities/ble_message.dart';

class BlePacketEncoder {
  List<int> encode(BleMessage message) {
    throw UnimplementedError('BLE packet encoding not implemented yet.');
  }
}